<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://vinayakpathak.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://vinayakpathak.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-09T14:55:57+00:00</updated><id>https://vinayakpathak.github.io/feed.xml</id><title type="html">Vinayak Pathak</title><subtitle></subtitle><entry><title type="html">On the Hardness of Bandit Learning</title><link href="https://vinayakpathak.github.io/blog/2026/paper-review-on-the-hardness-of-bandit-learning/" rel="alternate" type="text/html" title="On the Hardness of Bandit Learning"/><published>2026-07-07T00:00:00+00:00</published><updated>2026-07-07T00:00:00+00:00</updated><id>https://vinayakpathak.github.io/blog/2026/paper-review-on-the-hardness-of-bandit-learning</id><content type="html" xml:base="https://vinayakpathak.github.io/blog/2026/paper-review-on-the-hardness-of-bandit-learning/"><![CDATA[<p>Figuring out the computational complexity of learning is an important research question. The theory of learning has so far managed to prove that <a href="/blog/2026/computational-complexity-of-binary-classification/">very simple things are computationally hard</a> to learn. And yet, neural networks are learning things from real world data <a href="https://arxiv.org/abs/quant-ph/0502072">without having to solve NP-hard problems</a>. So something is happening in the computational complexity department that we do not understand.</p> <p>I came across <a href="https://arxiv.org/abs/2506.14746">this paper</a> by Nataly Brukhim, Aldo Pacchiano, Miroslav Dudik, and Robert Schapire that attempts to understand the computational hardness of bandit problems. While many “general” theorems are known about computational complexity of PAC-learning, not a lot was known for bandits. This paper has an interesting result.</p> <h2 id="what-computational-problem-is-bandit-learning">What Computational Problem is “Bandit Learning”?</h2> <p>Recall that in a bandit problem, you have a set \(\mathcal{A}\) of actions and a set \(\mathcal{F}\) of functions. Any \(f \in \mathcal{F}\) assigns an expected <em>reward</em> to each action, i.e., \(f: \mathcal{A}\rightarrow [0,1]\). An adversary picks some \(f^*\in\mathcal{F}\) and a learner interacts with it as follows. At time step \(t\), the learner picks action \(a_t \in \mathcal{A}\) and observes a reward \(r_t\) satisfying \(\mathbb{E}[r_t\mid a_t]=f^*(a_t)\). The learner does not know \(f^*\) but does know \(\mathcal{F}\). The goal of the learner is to minimize the pseudo-regret:</p> \[R(T) = T\max_{a\in\mathcal{A}} f^*(a) - \sum_{t=1}^T f^*(a_t)\] <p>We typically say that learning has been successful if there exists a way of picking the \(a_t\)’s so that \(R(T)\) is sublinear, i.e., \(\lim_{T\rightarrow\infty}\frac{R(T)}{T} = 0\). But now we want to understand the computational complexity, so we need to be more careful in defining the exact computational problem we intend to solve. In particular, what is the input and what is the output? And when do we say that a learner is “computationally efficient”?</p> <p>At each timestep \(t\), the learner needs to produce an action \(a\in\mathcal{A}\). This means we need an encoding of actions so the learner can write out the action it wants to take. For simplicity, we can assume that \(\mathcal{A}\) is the \(n\)-dimensional boolean hypercube \(\{0,1\}^n\) and thus an action is simply a boolean string of length \(n\). For now, this is enough to define the computational problem. At each time step \(t\), the learner gets to see the history of actions and rewards and needs to output \(a_t\in\mathcal{A}\). Thus it is reasonable to say that the learner is computationally efficient if it takes time and space \(\operatorname{poly}(n, t)\) at timestep \(t\). This rules out algorithms that scan all \(2^n\) possible actions before deciding on a single action.</p> <p>What about the regret? We have already agreed that we are going to call a bandit problem learnable if we can get regret to be sublinear in \(T\). But here we are asking when is a bandit problem <em>efficiently</em> learnable? This leads to two questions: 1) should we be satisfied by a mere sublinear dependence of regret on \(T\) or should we ask for something faster? 2) how should the regret depend on \(n\)?</p> <p>Suppose we say that regret should depend polynomially on \(n\) and sublinearly on \(T\), i.e., \(R(T) = \operatorname{poly}(n)o(T)\). Is this a good definition of computationally and statistically efficient bandit learning?</p> <p>I would say that this doesn’t feel very efficient. Moreover, this particular guarantee is somewhat trivial to obtain. We can basically brute force over the entire action space \(\mathcal{A}\) to find the best arm and then play it until eternity. Brute forcing can be done by playing one arm at a time, and thus requires only polynomial time per time step. We do need to maintain the best arm, and that can also be done with a polynomial amount of space. But why does this satisfy the regret guarantee of \(\operatorname{poly}(n)o(T)\)? Shouldn’t the dependence on \(n\) be exponential if we brute force? The reason is that if the dependence on \(T\) is allowed to grow arbitrarily slowly then we can hide some dependence on \(n\) within it. To see this, assume for now that we need to query each arm exactly once in order to identify the best arm. In general, this is not true, and the number of times we need to query each arm is a constant that depends on the noise. But for simplicity, we need to make \(2^n\) queries in total before we know the best arm. The strategy above gives a regret \(R(T) = \min(T, 2^n)\). The claim is that \(\min(T, 2^n) \leq O(n)\cdot\frac{T}{\log T}\). This can be easily verified by considering the two cases where \(T \leq 2^n\) and \(T &gt; 2^n\) and substituting.</p> <p>Then what is a good definition of computationally and statistically efficient bandit learning?</p> <h2 id="polynomial-regret-vs-best-arm-identification">Polynomial Regret vs Best Arm Identification</h2> <p>Another option is to be a bit more quantitative about exactly how quickly we want the average regret to go to zero. Earlier we were satisfied with a mere sublinear regret. But what if we require that \(R(T) \leq \operatorname{poly}(n)T^c\) for some fixed \(c &lt; 1\)? Let’s call this problem bandits with polynomial regret. After all, we do want statistical efficiency, and polynomial regret sounds a lot more efficient than a mere sublinear regret.</p> <p>For a fixed \(c &lt; 1\), another way to interpret this problem is to say that the learner is also given an \(\epsilon &lt; 1\) as input and needs to ensure that \(R(T)/T \leq \epsilon\) after a \(\operatorname{poly}(n, 1/\epsilon)\) amount of total computation. This is only possible if it runs in polynomial time per timestep and achieves a polynomial regret.</p> <p>The paper mostly considers a slightly different bandit problem, called the best arm identification problem. Here, we do not necessarily want a low regret, but merely want to identify an arm that is \(\epsilon\)-close to the optimal arm. That is, we want to find \(\hat{a}\in\mathcal{A}\) such that \(f^*(\hat{a}) \geq \max_{a\in\mathcal{A}} f^*(a) - \epsilon\). The \((\epsilon, \delta)\)-BAI problem is defined as follows.</p> <div class="definition"> <p><strong>Definition (\((\epsilon,\delta)\)-best-arm identification).</strong> Let \(\mathcal{A}\) be an action set and let \(\mathcal{F}\subseteq [0,1]^{\mathcal{A}}\) be a known class of reward functions. Nature chooses an unknown target function \(f^*\in\mathcal{F}\). A learner is given query access to \(f^*\), i.e., at each round \(t\), it chooses an action \(a_t\in\mathcal{A}\) and observes a reward \(r_t\) satisfying \(\mathbb{E}[r_t\mid a_t]=f^*(a_t)\).</p> <p>The learner solves the \((\epsilon,\delta)\)-BAI problem for \(\mathcal{F}\) with query complexity \(m(\epsilon,\delta)\) if, after at most \(m(\epsilon,\delta)\) queries, it outputs an action \(\hat{a}\in\mathcal{A}\) such that, for every \(f^*\in\mathcal{F}\), with probability at least \(1-\delta\),</p> \[ f^*(\hat{a}) \geq \sup_{a\in\mathcal{A}} f^*(a) - \epsilon. \] <p>We say that the learner is efficient if \(m(\epsilon, \delta)\), as well as its total running time, is \(\operatorname{poly}(n, 1/\epsilon, 1/\delta)\), where \(n\) is the representation length of the action set.</p> </div> <p>One can show that the polynomial regret problem defined above and the efficient BAI problem are equivalent. Indeed, if you have a learner for BAI, just run it to identify the best arm and then play that arm for the rest of the time steps. This gives us polynomial regret. On the other hand, if you have a polynomial regret learner, then run it for \(T\) rounds, where \(T\) will depend on \(\epsilon\) and \(\delta\) and pick a random arm uniformly among the \(T\) arms played. Using Markov inequality one can show this to be an \(\epsilon\)-optimal arm with high probability.</p> <p>In conclusion, the existence of an efficient \((\epsilon, \delta)\)-BAI learner is an excellent candidate for what one might call efficient bandit learnability.</p> <h2 id="when-is-efficient-bai-doable">When is Efficient BAI Doable?</h2> <p>So now, just like the paper, we can focus on the BAI problem. The main hardness theorem in the paper is stated in the noiseless setting, where rewards are deterministic functions of the arm. The authors later discuss how analogous noisy versions can be obtained, but we will stick with the noiseless setting here.</p> <p>Right away, we can realize that without any structure on \(\mathcal{F}\), nothing can be done. For example, suppose \(\mathcal{F}\) is the set of all singletons, i.e., for each \(a\in\mathcal{A}\), \(\mathcal{F}\) contains an \(f_a\) such that \(f_a(a) = 1\) and \(f_a(a') = 0\) for \(a' \neq a\). Nature has picked one of these functions and the learner does not know which one. In order to figure out the best arm, the learner needs to find the arm which gets the value of one by this function. This can only be done by querying all \(2^n\) arms and thus efficient BAI is impossible. This means it is important that at least the number of queries required for BAI should be polynomial. But is that sufficient? That is, can we think of a function class \(\mathcal{F}\) for which the number of queries required is small, and yet the amount of computation required is large?</p> <p>This is also not hard to construct. Let \(\mathcal{F}\) be the set of all polynomial sized Boolean formulas. To calculate \(f(a)\) we just evaluate the formula on the Boolean string corresponding to \(a\). Now consider that we are given an additional arm \(\star\) such that the reward for that arm is a real number encoding the formula. A polynomially-sized Boolean formula can be encoded with polynomially many bits, and we can arrange those bits in a way that the rewards lie within a small range, say \([1/4,1/3]\). Now the learner can query the arm \(\star\) once and infer the exact hidden \(f^*\). But to find the best arm, it needs to find an assignment to the \(n\) variables such that the formula evaluates to one. This is NP-hard. Thus even though BAI requires only one query, it is still <em>computationally</em> intractable.</p> <p>One might object that these are fairly unnatural instances of the bandit problem and thus it’s not that surprising that they are hard. The paper goes one step further and proves hardness under even stronger assumptions.</p> <p>One strategy for doing bandit learning or BAI could be something like the following:</p> <ol> <li>Try a bunch of arms and observe the rewards.</li> <li>Find a function in the function class that is consistent with those rewards.</li> <li>Assume that that is the true function and find the best arm for that function.</li> </ol> <p>To implement this strategy efficiently, we need to be able to solve #2 and #3 efficiently. The paper presents an example where both of them can be done efficiently and yet BAI is computationally intractable.</p> <p>The construction is quite neat. The example builds on top of the previous example. We now consider three kinds of arms. The first kind \(\mathcal{A}_1\) just contains the arm \(\star\). The second kind \(\mathcal{A}_2\) contains all Boolean strings of length \(n\). And the third kind \(\mathcal{A}_3\) contains \(2^n\) arms numbered from 1 to \(2^n\).</p> <p>Note that so far we were ignoring the encoding for the functions because the learner did not really need to output a function. However, solving #2 requires the algorithm to output a function and thus we need to specify how each function in the function class is represented using a finite encoding.</p> <p>The function class now is going to have two kinds of functions. First is the set of all polynomially sized Boolean formulas (actually the paper uses quadratically-sized for some technical reasons) and thus can be indexed by formula \(\phi\). The second set consists of functions indexed by the pair \(\phi, c\) where \(c \in [1..2^n]\) and \(\phi\) is some <em>satisfiable</em> formula.</p> <p>As before, all of the functions use the arm \(\star\) to encode their corresponding Boolean formula into a real number. For function \(f_{\phi, c}\) of the second kind, it assigns \(c \in \mathcal{A}_3\) the value 1, and something smaller to everything else. This means \(c\) is the best arm for \(f_{\phi, c}\) and thus can be easily read from the function’s encoding. Thus #3 is easily solved. We still need to specify what \(f_{\phi, c}\) assigns to all the other arms. For an arm in \(\mathcal{A}_3\) that’s not \(c\), \(f_{\phi, c}\) assigns it zero. For arms in \(\mathcal{A}_2\), if it is a satisfying assignment to \(\phi\), then the function gives it a value \(c/2^{n+1}\), and otherwise 0. Thus to actually find the \(c\) using only queries, one needs to find a satisfying assignment to \(\phi\).</p> <p>Finally, we can also see that #2 is tractable here. Given a sequence of (action, reward) pairs, we need to find a function consistent with them. If any of the arms happen to be \(\star\), then this is easy. Otherwise, we need to be able to find Boolean formulas that fit a set of given values. This is also easy.</p> <p>Taking a step back, what does this hardness show? It appears that the three step strategy outlined above is not enough for bandit learning. In particular, we need a tractable way to <em>explore</em>. That is, it is not enough to be able to “try a bunch of arms”. We need to be clever about exactly which arms to try and sometimes this cleverness might not be computationally tractable.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Figuring out the computational complexity of learning is an important research question. The theory of learning has so far managed to prove that very simple things are computationally hard to learn. And yet, neural networks are learning things from real world data without having to solve NP-hard problems. So something is happening in the computational complexity department that we do not understand.]]></summary></entry><entry><title type="html">Computational Complexity of Binary Classification</title><link href="https://vinayakpathak.github.io/blog/2026/computational-complexity-of-binary-classification/" rel="alternate" type="text/html" title="Computational Complexity of Binary Classification"/><published>2026-05-03T20:56:05+00:00</published><updated>2026-05-03T20:56:05+00:00</updated><id>https://vinayakpathak.github.io/blog/2026/computational-complexity-of-binary-classification</id><content type="html" xml:base="https://vinayakpathak.github.io/blog/2026/computational-complexity-of-binary-classification/"><![CDATA[<p>PAC learning for binary classification is a very well-studied problem and most variations in definitions turn out to be equivalent to each other if all we care about is sample complexity. Recall the following definitions:</p> <p>Let \(\mathcal{X}_n\) be an instance space, say \(\{0,1\}^n\), and let \(H_n \subseteq \{0,1\}^{\mathcal{X}_n}\) be a class of binary classifiers. A data distribution \(\mathcal{D}\) is any distribution over \(\mathcal{X}_n\times \{0,1\}\). For a classifier \(h:\mathcal{X}_n\rightarrow \{0,1\}\), define its error as</p> <div> \begin{equation} \operatorname{err}_{\mathcal{D}}(h) = \Pr_{(x,y)\sim \mathcal{D}}\left[h(x)\neq y\right]. \end{equation} </div> <p>The best error achievable by the class \(H_n\) under \(\mathcal{D}\) is</p> <div> \begin{equation} \operatorname{opt}_{\mathcal{D}}(H_n) = \inf_{h\in H_n} \operatorname{err}_{\mathcal{D}}(h). \end{equation} </div> <p>An agnostic PAC learner for \(H = \{H_n\}_{n\geq 1}\) is a randomized algorithm \(A\) such that for every \(n\), every distribution \(\mathcal{D}\) over \(\mathcal{X}_n\times \{0,1\}\), and every \(\epsilon,\delta&gt;0\), if \(A\) is given \(m(n,\epsilon,\delta)\) independent samples from \(\mathcal{D}\), then with probability at least \(1-\delta\) it outputs a classifier \(h\) satisfying</p> <div> \begin{equation} \operatorname{err}_{\mathcal{D}}(h) \leq \operatorname{opt}_{\mathcal{D}}(H_n)+\epsilon. \end{equation} </div> <p>the learner is <em>sample-efficient</em> if the number of samples it uses is polynomial in the natural parameters, i.e</p> <div> \begin{equation} m(n,\epsilon,\delta) \leq \operatorname{poly}\left(n,\frac{1}{\epsilon},\frac{1}{\delta}\right). \end{equation} </div> <p>If \(\operatorname{opt}_{\mathcal{D}}(H_n) = 0\), then we say that the problem is <em>realizable</em>.</p> <p>If we ask that the learner outputs a hypothesis \(h\in H_n\), then it’s called <em>proper</em> learning, otherwise it’s <em>improper</em> learning.</p> <p>Thus we have two axes: agnostic vs realizable, and proper vs improper. From standard results in learning theory, we know that sample-efficient learning in all four cases is characterized by the VC dimension of \(H_n\). This also means they are all equivalent to each other.</p> <p>However, things become a lot more messy when we study <em>computationally</em>-efficient learning. Here, we are not satisfied with a polynomial sample complexity, but also require that the learner runs in time polynomial in \(n, 1/\epsilon, 1/\delta\). As all things computational, these learning problems are hard to characterize. Indeed, no characterizations are know. But perhaps one can still investigate the relative hardness of the four versions. It’s obvious that agnostic cannot be easier than realizable (since realizable is a special case) and proper cannot be easier than improper (since a proper learner is also an improper learner). But which ones of these relationships are strict? Can we show, for example, that proper and improper are actually equivalent?</p> <p>An old and beautiful result from Pitt and Valiant showed that in the realizable case, proper and improper are genuinely different. They gave an example of a learning problem that was computationally hard in the proper case (unless RP = NP), but efficient in the improper case. The problem is also a fairly natural one.</p> <p>Let \(H_n\) be the class of DNF formulas in \(n\) variables with \(k\) terms. That is, each hypothesis in \(H_n\) is of the form \(T_1\lor\cdots\lor T_k\) where each \(T_i\) is a conjunction of literals. Pitt and Valiant showed that this class is hard to learn properly in the realizable case unless RP = NP. We will get to the proof later. First let’s see how it can successfully be learned in the improper case. We begin by converting the DNF formula into its CNF form. This can be done by picking one literal from each \(T_i\) and taking their disjunction to form one clause of the CNF. Then taking a conjunction of all clauses thus formed, we get the equivalent CNF formula<sup id="fnref:dnf-to-cnf" role="doc-noteref"><a href="#fn:dnf-to-cnf" class="footnote" rel="footnote">1</a></sup>. Since there are \(k\)-terms in the original DNF, we get that the CNF will have \(k\) literals per clause.</p> <p>Interestingly, even though we associate CNF formulas (with more than two literals per clause) with various kinds of hardness results, when it comes to PAC learning, these things can be learned efficiently in the realizable setting. This is because the total number of clauses of size at most \(k\) that one can create using \(n\) variables is \(O(n^k)\) and thus the total number of formulas one can create with these clauses is at most \(2^{O(n^k)}\). This is a finite hypothesis class and thus has a VC dimension of at most \(O(n^k)\) and thus running ERM on the training examples should successfully learn it. ERM can also be run in polynomial time. The idea is to go through the positive examples and keep exactly the clauses that are satisfied by all of them. The conjunction of all clauses still consistent with the positive examples constitutes the final CNF formula. Since the sample is realizable, every negative example must falsify at least one of these retained clauses, and hence the final CNF labels the whole sample correctly.</p> <p>Ok, so k-CNF formulas can be efficiently and properly PAC-learned in the realizable setting. This sounds like a very large class of hypotheses. What goes wrong when we try to learn DNF formulas instead? One can show that a DNF formula with just two conjunctions \(T_1\lor T_2\) is impossible to learn properly in the realizable setting unless \(RP=NP\).</p> <p>First note the following general result. Consider doing proper learning for a hypothesis class \(H\) under the realizable setting. Consider also the computational problem: given labeled points \((x_1, y_1),\ldots , (x_n, y_n)\), decide if there exists some \(h\in H\) such that \(h(x_i) = y_i\) for all \(i\). It is easy to see that if a computationally efficient proper PAC learner exists in the realizable setting then this decision problem is in RP. Indeed, let \(D\) be the uniform distribution over \((x_1, y_1), \ldots , (x_n, y_n)\), and invoke the proper PAC learner with \(\epsilon &lt; 1/n\) and \(\delta = 1/3\). If an \(h\in H\) exists that correctly labels all the examples, then we are in the realizable setting, and the proper PAC learner returns a hypothesis of error less than \(1/n\) with probability at least \(2/3\). Under the uniform distribution over the sample points, this means it labels every point correctly. If no such \(h\) exists, then no matter what the learner returns, the final verification step will reject it. Thus we can simply run the PAC learner and check whether the returned hypothesis labels all points correctly. If it does, then we say yes, otherwise we say no. In the yes case, we say yes with probability at least \(2/3\), and in the no case we always say no. Thus we have found an algorithm in RP for the decision problem.</p> <p>We can apply this argument to show hardness for proper PAC learning in the realizable case. For a given hypothesis class, we just need to prove that the decision problem of checking whether a hypothesis exists that correctly labels all input examples is hard. For the hypothesis class of length-2 DNF formulas, we can reduce an NP-complete problem to it, thus proving that efficient proper learning is possible only if RP = NP.</p> <p>The reduction is from the set splitting problem, which is known to be NP-complete. An instance of the set splitting problem is a set system, i.e., a set \(S\) and a set \(F\) of its subsets, and the task is to decide whether \(S\) can be split into two disjoint parts such that each subset in \(F\) contains elements from each part. We may assume each set in \(F\) has size at least two; set splitting remains NP-complete under this restriction. Given an instance of the set splitting problem, we construct the following labeled examples.</p> <p>Let \(S=\{s_1,\ldots,s_n\}\). For each element \(s_i\), we have a variable \(x_i\). For each \(s_i\in S\), create the point \(p_i=(1,\ldots,1,0,1,\ldots,1)\), where the unique zero is in coordinate \(i\), and label it \(1\). Next, for each \(f\in F\), create the point \(q_f\) where</p> <div> \begin{equation} (q_f)_i = \begin{cases} 0 &amp; \text{if } s_i\in f,\\ 1 &amp; \text{otherwise}. \end{cases} \end{equation} </div> <p>Label each \(q_f\) by \(0\). We now show that a valid partition of \(S\) exists if and only if there is a 2-term DNF formula that correctly labels all these examples.</p> <p>First suppose that \(S\) has a valid split \(S=S_1\cup S_2\). Define</p> <div> \begin{equation} T_1=\bigwedge_{s_i\in S_2} x_i, \qquad T_2=\bigwedge_{s_i\in S_1} x_i, \qquad h=T_1\lor T_2. \end{equation} </div> <p>Consider a positive example \(p_i\). If \(s_i\in S_1\), then the unique zero of \(p_i\) does not appear in \(T_1\), so \(T_1(p_i)=1\). Similarly, if \(s_i\in S_2\), then \(T_2(p_i)=1\). Thus \(h(p_i)=1\) for every positive example.</p> <p>Now consider a negative example \(q_f\). Since the split is valid, \(f\) contains some element of \(S_1\) and some element of \(S_2\). Therefore \(q_f\) has a zero in a variable appearing in \(T_2\) and a zero in a variable appearing in \(T_1\). Hence both terms are false, so \(h(q_f)=0\).</p> <p>For the converse, suppose there is a 2-term DNF \(h=T_1\lor T_2\) that correctly labels all the examples. We may assume without loss of generality that \(T_1\) and \(T_2\) contain only positive literals. Indeed, a term with two or more negated literals cannot satisfy any positive example \(p_i\), because each \(p_i\) has only one zero. Such a term can simply be deleted. A term with exactly one negated literal \(\lnot x_i\) can only help on the positive example \(p_i\); it can be replaced by the positive term \(\bigwedge_{j\neq i}x_j\), which still accepts \(p_i\) and rejects every negative example \(q_f\), since every set \(f\) in a nontrivial set-splitting instance has size at least two.</p> <p>So write</p> <div> \begin{equation} T_1=\bigwedge_{i\in A_1}x_i, \qquad T_2=\bigwedge_{i\in A_2}x_i. \end{equation} </div> <p>Since \(p_i\) is labeled \(1\), it must satisfy at least one of the two terms. Equivalently, for every \(i\), either \(i\notin A_1\) or \(i\notin A_2\). We define a split as follows: put \(s_i\) in \(S_1\) if \(i\notin A_1\), and put it in \(S_2\) otherwise. This is well-defined because if \(i\in A_1\), then the fact that \(p_i\) is positive forces \(i\notin A_2\).</p> <p>It remains to check that every \(f\in F\) is split. Since \(q_f\) is labeled \(0\), both terms must reject it. Since \(T_1(q_f)=0\), there is some \(i\in A_1\) with \(s_i\in f\). By the definition of the split, this element lies in \(S_2\). Similarly, since \(T_2(q_f)=0\), there is some \(j\in A_2\) with \(s_j\in f\), and this element lies in \(S_1\). Therefore \(f\) intersects both \(S_1\) and \(S_2\).</p> <p>Thus the examples are consistent with a 2-term DNF if and only if the original set-splitting instance is satisfiable. Since set splitting is NP-complete, the consistency problem for 2-term DNF is NP-hard. By the previous argument, an efficient proper PAC learner for 2-term DNF in the realizable setting would put this NP-hard decision problem in RP, and hence would imply \(\mathrm{RP}=\mathrm{NP}\).</p> <p>Ok, so now we have for the realizable case, an example where proper learning is hard but improper is easy. Thus being able to do improper realizable learning efficiently does not imply we can also do proper realizable learning. But what if we can do improper <em>agnostic</em> learning efficiently? Can that imply efficient learner for proper realizable learning too? It is kind of a strange thing to do to compare agnostic improper against realizable proper, and thus I couldn’t find a paper that explicitly studies this problem. Thus I deployed GPT 5.5 Pro on it and it found an amazingly elegant counterexample. It says it’s not new and cites a few papers that have had “similar ideas”. But looking at those papers, I don’t find their ideas to be all that similar. So either GPT is being humble or it’s just not citing the correct thing. But the counterexample is quite simple and it also subsumes the previous counterexample. Indeed, since we get an example where agnostic improper learning is easy but realizable proper learning is hard, the same example also proves that it’s possible for realizable improper learning to be easy while realizable proper learning is hard.</p> <p>Here is the counterexample.</p> <p>For each \(n\), let \(X_n\) be the set of all 3-CNF clauses over variables \(x_1,\ldots,x_n\). Thus, an element of \(X_n\) is a disjunction of three literals. Let’s allow repeated literals just so that every clause has exactly three slots. Thus the domain is small, i.e., \(\lvert X_n\rvert \leq O(n^3)\).</p> <p>Now for every assignment \(a\in\{0,1\}^n\), define a classifier \(h_a:X_n\rightarrow \{0,1\}\) by</p> <div> \begin{equation} h_a(C)=1 \quad\Longleftrightarrow\quad a\text{ satisfies the clause }C. \end{equation} </div> <p>Let \(H_n=\{h_a:a\in\{0,1\}^n\}\). We can sort of see why this might be hard to learn properly. Indeed, if there is an efficient proper PAC learner for \(H\) in the realizable setting, then we can use that to solve 3-SAT using a similar reduction as earlier.</p> <p>Take a 3-CNF formula \(\varphi = C_1\land C_2\land\cdots\land C_m\).</p> <p>Define a distribution \(D_\varphi\) over labeled examples by choosing \(j\in[m]\) uniformly at random and outputting \((C_j,1)\). If \(\varphi\) is satisfiable, then there is an assignment \(a^\star\) satisfying every clause. Therefore \(h_{a^\star}(C_j)=1\) for every \(j\), and hence \(D_\varphi\) is realizable by \(H_n\).</p> <p>Run the assumed proper realizable learner with \(\epsilon&lt;1/m\) and \(\delta=1/3\). If \(\varphi\) is satisfiable, then with probability at least \(2/3\) it returns a proper hypothesis \(h\in H_n\) whose error under \(D_\varphi\) is less than \(1/m\). But under \(D_\varphi\), even one unsatisfied clause gives error at least \(1/m\). Therefore the returned hypothesis must satisfy \(h(C_j) = 1\) for every \(j\).</p> <p>Since the learner is proper, \(h=h_a\) for some assignment \(a\). Thus \(a\) satisfies every clause of \(\varphi\).</p> <p>So we can decide 3-SAT as follows. Given \(\varphi\), simulate samples from \(D_\varphi\), run the proper learner, and check whether the returned proper hypothesis labels every clause by \(1\). If \(\varphi\) is satisfiable, this accepts with probability at least \(2/3\). If \(\varphi\) is unsatisfiable, no assignment satisfies every clause, so no proper hypothesis can label every \(C_j\) by \(1\), and the algorithm never accepts. Thus 3-SAT would be in RP.</p> <p>On the other hand, if we allow improper learning, then this becomes efficiently learnable in the agnostic case. Let \(F_n=\{0,1\}^{X_n}\) be the class of all Boolean functions on \(X_n\) and run proper learning wrt this class. Since the original class is a subset, this would give us an improper learner. Learning wrt \(F_n\) is easy because first of all \(\log \lvert F_n\rvert = \lvert X_n\rvert = O(n^3)\)</p> <p>Thus ERM suffices for learning. Now given a labeled sample of clauses, running ERM over \(F_n\) is trivial. For each clause \(C\in X_n\), look at all sample points equal to \(C\) and label \(C\) by the empirical majority label, breaking ties arbitrarily. This gives a lookup table \(\hat{g}:X_n\rightarrow \{0,1\}\).</p> <p>Therefore, assuming \(\mathrm{NP}\nsubseteq \mathrm{RP}\), efficient improper agnostic learning does not imply efficient proper realizable learning.</p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:dnf-to-cnf" role="doc-endnote"> <p>This works because in Boolean algebra, AND distributes over OR’s. <a href="#fnref:dnf-to-cnf" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><summary type="html"><![CDATA[PAC learning for binary classification is a very well-studied problem and most variations in definitions turn out to be equivalent to each other if all we care about is sample complexity. Recall the following definitions:]]></summary></entry><entry><title type="html">Paper Review: How Does Machine Learning Manage Complexity?</title><link href="https://vinayakpathak.github.io/blog/2026/paper-review-how-does-machine-learning-manage-complexity/" rel="alternate" type="text/html" title="Paper Review: How Does Machine Learning Manage Complexity?"/><published>2026-04-28T00:00:00+00:00</published><updated>2026-04-28T00:00:00+00:00</updated><id>https://vinayakpathak.github.io/blog/2026/paper-review-how-does-machine-learning-manage-complexity</id><content type="html" xml:base="https://vinayakpathak.github.io/blog/2026/paper-review-how-does-machine-learning-manage-complexity/"><![CDATA[<p>I recently came across this interesting <a href="https://arxiv.org/pdf/2604.07233">paper</a> by Lance Fortnow titled “How Does Machine Learning Manage Complexity?” I am very happy to see complexity theorists getting into some ML theory. If nothing else, I am looking forward to seeing more papers that start by saying:</p> <blockquote> <p>In this paper all distributions will be over \(\{0, 1\}^n\).</p> </blockquote> <p>Instead of “let \(\mathcal{X}\) be a Polish space equipped with its Borel \(\sigma\)-algebra.”</p> <p>The paper makes some interesting observations at the intersection of ML theory and complexity theory and here are two I want to make a note of for the future.</p> <h4 id="what-complexity-class-does-a-language-model-belong-to">What complexity class does a language model belong to?</h4> <p>Assume that language models operate on a vocabulary of size two, i.e., \(\{0,1\}\). Consider a language model that takes a sequence of length \(n\) as input and produces the probability that the next token is a one. What complexity class is this computation in? Of course, since it does successfully happen in the real world, it must be “efficient”. But is it in P?</p> <p>The paper makes the argument that it’s better to think of it as a polynomial-sized circuit with non-uniformity allowed aka P/poly. The neural network architecture kind of looks like a polynomial-sized circuit so it makes sense to think of it as a circuit. But why should we allow it to be non-uniform? The argument is that the training process is long and arduous and so we don’t want to make an assumption that that is also efficient, certainly not efficient in terms of the input size \(n\). The training process finds the weights and thus creates the circuit. We can think of the training process as the thing that finds the polynomial-sized advice.</p> <h4 id="how-are-neural-networks-able-to-represent-real-world-distributions">How are neural networks able to represent real-world distributions?</h4> <p>The paper points out a paradox. Real-world distributions are efficiently sampleable. So let’s say they are P-sampleable, i.e., there exists a randomized algorithm that runs in time polynomial in \(n\) and samples a binary string of length \(n\) from the real-world distribution. However, the distribution over n-bit strings represented<sup id="fnref:nn" role="doc-noteref"><a href="#fn:nn" class="footnote" rel="footnote">1</a></sup> by a neural network is P/poly-computable as opposed to merely sampleable. This is because given any binary string, one can compute the probability assigned to it by the neural network<sup id="fnref:teacher-forcing" role="doc-noteref"><a href="#fn:teacher-forcing" class="footnote" rel="footnote">2</a></sup>, and we are assuming that the inference algorithm is in P/poly. But we know that there are many P-sampleable distributions that are not P/poly-computable. So how does a neural network represent them?</p> <p>First, what are examples of P-sampleable distributions that are not P/poly-computable?</p> <p>Consider, for example, a distribution over \(2n\)-bit strings where the last \(n\) bits \(x\) come from a P-sampleable distribution, and the first \(n\) bits are \(f(x)\) where \(f\) is a one-way function. This is clearly P-sampleable. However, if it were also P-computable, then we would be able to invert \(f\). Indeed, given \(f(x)\), we could construct \(x\) one bit at a time and check if the concatenation of \(f(x)\) and the partially constructed \(x\) has a non-zero probability. Thus first, we check the probability of \(f(x)+1\) and \(f(x)+0\) (where \(+\) denotes concatenation), and if \(f(x)+1\) is non-zero, we check \(f(x)+11\) and \(f(x)+10\), and so on.</p> <p>Another fairly natural example comes from pseudo-random generators (PRG). A PRG for n-bit strings is an efficiently computable function \(f: \{0,1\}^l \rightarrow \{0,1\}^n\) for some \(l &lt; n\) that takes a (uniformly) randomly generated \(l\)-bit “seed” and produces an \(n\)-bit string that looks as good as uniformly random to every efficient algorithm. Let \(\text{PRG}\) denote the distribution induced on \(\{0,1\}^n\) by a given pseudo-random generator, and let \(U\) be the uniform distribution over \(\{0,1\}^n\). Then it is a successful PRG if for any (randomized) P/poly algorithm \(A\), and for every \(k&gt;0\), we have that:</p> <p>\begin{equation} \label{eq:prg} \left|\Pr_{x \sim \text{PRG}}\left[A(x)=1\right] - \Pr_{x \sim U}\left[A(x)=1\right]\right| \leq \frac{1}{n^k}. \end{equation}</p> <p>It is standard to define \(\operatorname{negl}(n)\) as a function such that for every \(k&gt;0\), \(\operatorname{negl}(n) \leq \frac{1}{n^k}\). Thus one could write the above equation with the right hand side replaced by \(\operatorname{negl}(n)\).</p> <p>Clearly, a PRG is P-sampleable. Suppose it was also P/poly-computable. Then consider an algorithm \(A\) that takes \(n\)-bit strings as inputs such that \(A(x) = 1\) iff \(\Pr_\text{PRG}[x] &gt; 0\). Thus \(\Pr_{x \sim \text{PRG}}\left[A(x)=1\right] = 1\), but \(\Pr_{x \sim U}\left[A(x)=1\right] \leq 2^{-(n-l)}\). This clearly violates \eqref{eq:prg}. Thus the distribution could not have been P/poly-computable.</p> <p>Now that we have seen some examples of P-sampleable distributions that are not P/poly-computable, let’s get back to the paradox. How does the paper resolve this? The resolution is not that neural networks can learn a P/poly-computable distribution that is indistinguishable from the real one if such a distribution exists. The paper formalizes this using the following (paraphrased) theorem:</p> <p><strong>Theorem.</strong> Let \(D\) be a P-sampleable distribution over \(\{0,1\}^n\) and let \(\mu\) be a distribution learned by minimizing \(\operatorname{KL}(D \mathrel{\Vert} \mu)\). Suppose there exists a P/poly-computable distribution \(\nu\) such that \(\nu\) is indistinguishable from \(D\) in the sense of \eqref{eq:prg}. Then \(\mu\) is very close to \(\nu\) in KL-divergence, i.e., \(\operatorname{KL}(\nu \mathrel{\Vert} \mu)&lt;\operatorname{negl}(n)\). In particular, \(\mu\) is also indistinguishable from \(D\).</p> <p>Thus, for example, if \(D\) is the distribution generated by a PRG, then the model ends up learning something that’s indistinguishable from a uniform distribution.</p> <p><strong>Proof.</strong> Let’s prove this for the case when both \(\nu\) and \(\mu\) have bounded max-entropy, i.e., there exists a \(k&gt;0\) such that \(\log\frac{1}{\mu(x)}\leq n^k\) and \(\log\frac{1}{\nu(x)}\leq n^k\) for all \(x\). The paper extends it to unbounded max-entropy as well.</p> <p>Define \(f(x) = \log\frac{\nu(x)}{\mu(x)}\). Note that \(f(x)\) is P/poly-computable. Also, \(\lvert f(x)\rvert\leq n^k\), because both \(\log\frac{1}{\mu(x)}\) and \(\log\frac{1}{\nu(x)}\) are between \(0\) and \(n^k\). This means \(\lvert\mathbb{E}_{x\sim D} f(x) - \mathbb{E}_{x\sim \nu} f(x)\rvert\leq\operatorname{negl}(n)\) because otherwise the process \(A(x)\) that outputs 1 with probability \(\frac{f(x)+n^k}{2n^k}\) would be able to distinguish between \(D\) and \(\nu\). Also, note that \(\mathbb{E}_{x\sim D} f(x) = \operatorname{KL}(D \mathrel{\Vert} \mu) - \operatorname{KL}(D \mathrel{\Vert} \nu)\leq 0\), because \(\mu\) was found by minimizing the KL with \(D\). Thus \(\operatorname{KL}(\nu \mathrel{\Vert} \mu)=\mathbb{E}_{x\sim \nu} f(x)\leq\operatorname{negl}(n)\).</p> <p>Finally, by Pinsker’s inequality, \(d_{\mathrm{TV}}(\nu,\mu)\leq \sqrt{\frac{1}{2}\operatorname{KL}(\nu \mathrel{\Vert} \mu)}\). Since the square root of a negligible function is still negligible, \(\nu\) and \(\mu\) are negligibly close in total variation distance. Thus, for every efficient distinguisher \(A\), the distinguishing gap between \(D\) and \(\mu\) is at most the distinguishing gap between \(D\) and \(\nu\) plus the total variation distance between \(\nu\) and \(\mu\), which is negligible.</p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:nn" role="doc-endnote"> <p>A language model models \(p(y\mid x)\) where \(x\) is any \(k\)-bit string with \(k&lt;n\), and \(y\in\{0,1\}\). This, together with a distribution on the first bit, induces a distribution over all \(n\)-bit strings. We say that this is the distribution represented by the language model. <a href="#fnref:nn" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:teacher-forcing" role="doc-endnote"> <p>This is fairly straightforward for transformer models. For any string \(x\), you can simply feed it to the model as input, and read off the probability in front of each bit. Multiplying all of them gives you the probability of the entire string. <a href="#fnref:teacher-forcing" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><summary type="html"><![CDATA[I recently came across this interesting paper by Lance Fortnow titled “How Does Machine Learning Manage Complexity?” I am very happy to see complexity theorists getting into some ML theory. If nothing else, I am looking forward to seeing more papers that start by saying:]]></summary></entry><entry><title type="html">Benign overfitting in linear regression</title><link href="https://vinayakpathak.github.io/blog/2026/benign-overfitting-in-linear-regression/" rel="alternate" type="text/html" title="Benign overfitting in linear regression"/><published>2026-04-20T22:24:50+00:00</published><updated>2026-04-20T22:24:50+00:00</updated><id>https://vinayakpathak.github.io/blog/2026/benign-overfitting-in-linear-regression</id><content type="html" xml:base="https://vinayakpathak.github.io/blog/2026/benign-overfitting-in-linear-regression/"><![CDATA[<p>This blog post is based on <a href="https://arxiv.org/abs/1906.11300">this paper</a>, in particular, its exposition in <a href="https://arxiv.org/abs/2103.09177">this survey</a>.</p> <p>The story of machine learning theory is that a long long time ago we thought uniform convergence was crucial for generalization. That is, if you had a hypothesis class (for example, the set of all neural networks with a certain specific architecture), and you picked one hypothesis from that class based on the training set, then the main tool we had to show that its <em>test loss</em> was small was to show that: a) its <em>training loss</em> was small, and b) the hypothesis class had the property that with high probability over the sampling of the training set, every hypothesis in that class had a small difference between its test loss and train loss. Or in other words, proving generalization relied on proving that \(\lvert \hat{L}(h) - L(h) \rvert \leq \epsilon\) with high probability.</p> <p>But then came along the “rethinking” <a href="https://arxiv.org/abs/1611.03530">paper</a>, and threw a wrench in the plan. It showed that you could train a neural network on <em>noisy</em> data, i.e., data where you intentionally corrupt the labels of a \(p\) fraction of the data points, and the network ended up perfectly fitting the training set and still getting the optimal loss on the test set. That is, you had \(\hat{L}(h) = 0\) and \(L(h) = p/2\) (assuming it’s binary classification, it gets error 1/2 on $p$ fraction of the points). Thus a good test error was being achieved despite the previous strategy failing. Thus it was suggested that we needed some rethinking.</p> <p>Achieving \(\hat{L}(h) = 0\) on noisy data is pretty much the definition of overfitting. And yet, as the experiments in the “rethinking” paper showed, the network was not overfitting. This phenomenon was later named <em>benign overfitting</em>.</p> <p>It is actually not very hard to imagine scenarios where benign overfitting can happen. Let’s go back to uniform convergence and pick a hypothesis class where uniform convergence does happen. Thus, given a training set, you pick a hypothesis from the class that minimizes the training error. Since the data here is noisy, you won’t get a small training error. However, as long as your sample size is large enough, you do have the guarantee that the test error is close to the training error, and hence close to the optimum test error. But how do we find a hypothesis that does optimally on the test set while achieving <em>zero</em> loss on the training set? This is quite simple. Suppose \(\hat{h}\) is the result of minimizing the training loss. Consider a hypothesis \(h'\) such that for any \((x,y)\) that’s actually in the training set, \(h'(x) = y\), and for everything else \(h(x) = \hat{h}(x)\). For non-discrete problems, the training set is of measure zero, and therefore \(h'\) has the same test loss as \(\hat{h}\) and yet zero loss on the training set.</p> <p>So the mystery isn’t that benign overfitting happens, but that benign overfitting happens without adding this extra hypothesis-modification step in the end. In reality, we don’t train a network and then modify it to fit the training data perfectly. We simply train a network and the trained network exhibits this strange property. How does that happen?</p> <p><a href="https://arxiv.org/abs/1906.11300">Bartlett et al</a> studied this phenomenon in the simplest setting: linear regression. Imagine you are given \(n\) points \(x_1,\ldots , x_n \in \mathbb{R}^d\) along with their \(y\) values \(y_1, \ldots , y_n\in \mathbb{R}\). Suppose that \(n &lt; d\), thus a zero-training-loss solution can be achieved. Consider the training algorithm that picks the minimum norm solution among this set, i.e., we pick</p> <div> \begin{equation} \label{eq:interp} \hat{\theta} = \arg\min_\theta \{\lVert\theta\rVert^2: \theta^Tx_i = y_i, \forall i\leq n\} \end{equation} </div> <p>Since this <em>interpolates</em>, i.e., achieves zero loss on the training set, it’s unclear a priori how it would perform on test set. We can’t really use the uniform convergence strategy here. However, we can do some linear algebra magic and see that an interesting picture emerges.</p> <p>Note, first of all, that the \(n &lt; d\) assumption is the culprit here. It is what’s causing all this weirdness. For example, if \(n &gt; \Omega(d)\) satisfied whatever sample complexity requirements one gets from calculating the Rademacher complexity of the linear regression problem, then we would have that training loss and test loss were close to each other, and uniform convergence would perfectly explain the generalization. So let’s just consider a lower dimensional part of the problem for the time being. Truncate the \(x\)’s to their first \(k\) coordinates. Let \(X\) be the original \(n\times d\) <em>design matrix</em> and let \(X_{\leq k}\) be the \(n\times k\) design matrix you get by keeping only the first \(k\) coordinates, and \(X_{&gt;k}\) be the remaining coordinates. Now consider the following ridge regression problem in \(\mathbb{R}^k\):</p> <div> \begin{equation} \label{eq:ridge} \hat{\theta}_{\leq k} = \arg\min_{\theta\in\mathbb{R}^k} \lVert X_{\leq k}\theta - y\rVert^2 + \gamma\lVert\theta\rVert^2 \end{equation} </div> <p>One can show that under certain assumptions on \(X\), the predictor that solves \eqref{eq:ridge} and outputs \(\hat{\theta}_{\leq k}^Tx_{\leq k}\) for any \(x\) produces very similar outputs as the predictor given by the solution to \eqref{eq:interp}. We will worry about the assumptions later, but if this is indeed true, we get that the interpolating solution is really just a lower dimensional ridge regression in disguise. Of course, a low-dimensional ridge regression guarantees uniform convergence but does not give us interpolation. This is where the remaining \(d-k\) coordinates help. They provide enough leeway to fit the training points exactly without impacting the generalization loss by much.</p> <p>So in a sense this is similar to first learning using uniform convergence and then artificially changing the predictor so it fits the training points perfectly as described earlier. However, in this case, the artificial perturbation of the predictor is still a part of the original learning algorithm as opposed to an additional step.</p> <p>Now, to see when and why \eqref{eq:interp} and \eqref{eq:ridge} are approximately equivalent, write \eqref{eq:interp} as:</p> <div> \begin{equation} \label{eq:interp-split} \hat{\theta} = \arg\min_{\theta_{\leq k}, \theta_{&gt;k}} \{\lVert\theta_{\leq k}\rVert^2 + \lVert\theta_{&gt;k}\rVert^2: X_{\leq k}\theta_{\leq k} + X_{&gt;k}\theta_{&gt;k} = y\} \end{equation} </div> <p>If we fix \(\theta_{\leq k}\), then the problem becomes:</p> <div> \begin{equation} \hat{\theta}_{&gt;k} = \arg\min_{\theta_{&gt;k}} \{\lVert\theta_{&gt;k}\rVert^2: X_{&gt;k}\theta_{&gt;k} = y - X_{\leq k}\theta_{\leq k}\} \end{equation} </div> <p>This has the closed form solution which satisfies:</p> <div> \begin{equation} \lVert\hat{\theta}_{&gt;k}\rVert^2 = (y - X_{\leq k}\theta_{\leq k})^T(X_{&gt;k}X_{&gt;k}^T)^{-1}(y - X_{\leq k}\theta_{\leq k}) \end{equation} </div> <p>Plugging this back into \eqref{eq:interp-split}, we get that:</p> <div> \begin{equation} \hat{\theta}_{\leq k} = \arg\min_{\theta_{\leq k}} \{\lVert\theta_{\leq k}\rVert^2 + (y - X_{\leq k}\theta_{\leq k})^T(X_{&gt;k}X_{&gt;k}^T)^{-1}(y - X_{\leq k}\theta_{\leq k})\} \end{equation} </div> <p>If we could say that \((y - X_{\leq k}\theta_{\leq k})^T(X_{&gt;k}X_{&gt;k}^T)^{-1}(y - X_{\leq k}\theta_{\leq k})\approx \lVert y - X_{\leq k}\theta_{\leq k}\rVert^2/\gamma\), then we get exactly \eqref{eq:ridge}. But when can we say this? Basically we need \(X_{&gt;k}X_{&gt;k}^T\) to sort of behave like a scaled \(I\). The paper shows certain distributional assumptions for which this holds with high probability. It eventually boils down to the eigendecomposition of \(\mathbb{E}[xx^T]\) and how flat its tail is. If the tail is flat enough, then one can show that the above holds once we write everything in the basis of the eigenvectors of \(\mathbb{E}[xx^T]\).</p>]]></content><author><name></name></author><summary type="html"><![CDATA[This blog post is based on this paper, in particular, its exposition in this survey.]]></summary></entry><entry><title type="html">Where do square roots come from?</title><link href="https://vinayakpathak.github.io/blog/2026/where-do-square-roots-come-from/" rel="alternate" type="text/html" title="Where do square roots come from?"/><published>2026-04-09T17:06:56+00:00</published><updated>2026-04-09T17:06:56+00:00</updated><id>https://vinayakpathak.github.io/blog/2026/where-do-square-roots-come-from</id><content type="html" xml:base="https://vinayakpathak.github.io/blog/2026/where-do-square-roots-come-from/"><![CDATA[<p>At some point I used to do computational/combinatorial geometry and I’d have square roots appear in my calculations a lot. Most of the time they appeared due to the Pythagoras theorem. For example, the long diagonal of the \(n\)-dimensional cube circumscribing the unit ball, has length \(2\sqrt{n}\).</p> <p><img src="/assets/img/blog/mathematics-in-flight.png" alt="Mathematics in flight" class="img-fluid rounded z-depth-1" width="40%"/></p> <p>Now I do learning theory, and square roots appear a lot still. And now they appear in convergence bounds. For example, with 99% probability, the empirical mean approximates the true mean of a (finite variance) random variable to within an additive error \(O(1/\sqrt{n})\). Something I recently realized is that both square roots appear for almost the same reason. They are both consequences of things being an inner product space.</p> <p>An <a href="https://en.wikipedia.org/wiki/Inner_product_space">inner product space</a> is a vector space together with an inner product defined on pairs of vectors, denoted \(\langle u, v \rangle\). Once you have inner products, you also get norms by taking the inner product of a vector by itself. That is, \(\lVert u \rVert^2 = \langle u, u \rangle\). Once you have norms, square roots start to sound like things you might frequently encounter. Indeed, if inner product is norm squared, then norm is the square root of the inner product. However, the story becomes interesting only once you start calculating norms of <em>sums</em> of vectors.</p> <p>An inner product also satisfies linearity. In particular,</p> <p>\begin{equation} \langle u+v, w \rangle = \langle u, w \rangle + \langle v, w \rangle \end{equation}</p> <p>This, combined with symmetry, gives us a way to calculate the norm of sums of vectors. Thus, \(\lVert u + v \rVert^2 = \langle u+v, u+v\rangle = \lVert u \rVert^2 + \lVert v \rVert^2 + 2\langle u, v\rangle\), and in particular, when \(\langle u, v \rangle = 0\), we can simply write,</p> <p>\begin{equation}\label{eq:pythagoras}\lVert u + v \rVert^2 = \lVert u \rVert^2 + \lVert v \rVert^2\end{equation}</p> <p>Now if we consider the inner product space defined by vectors in a Euclidean space, then this gives us exactly the Pythagoras theorem<sup id="fnref:pythagoras" role="doc-noteref"><a href="#fn:pythagoras" class="footnote" rel="footnote">1</a></sup>. We don’t need to stop at two vectors. We can add \(n\) orthogonal vectors, each of unit length, and what we end up with will have a length \(\sqrt{n}\). This is precisely what gives us the length of the diagonal of a cube in n-dimensions.</p> <p>Why do we get \(\sqrt{n}\) in convergence bounds?</p> <p>Another interesting inner product space one can consider is the space of random variables. For any two random variables \(X\) and \(Y\), define their inner product as \(\langle X, Y \rangle = \mathbb{E}[XY]\). One can check that this indeed forms an inner product space.</p> <p>Now consider the task of approximating the true mean of a random variable \(X\) using iid samples \(X_1,\ldots , X_n\) from it. Consider the difference between the empirical mean and the true mean, i.e., \(\frac{1}{n}\sum_{i=1}^n X_i - \mathbb{E}X\). Write this as \(\frac{1}{n}\sum_{i=1}^n\left(X_i - \mathbb{E}X \right) = \frac{1}{n}\sum_{i=1}^n Z_i\) where \(Z_i = X_i - \mathbb{E}X\). Now think of \(Z_i\)’s as vectors in the inner product space of random variables and note that \(\langle Z_i, Z_j \rangle = \mathbb{E}[Z_iZ_j] = \mathbb{E}[(X_i-\mathbb{E}X)(X_j-\mathbb{E}X)] = \text{Cov}(X_i, X_j)\). Also, \(\lVert Z_i \rVert^2 = \text{Var}(X_i)\). But since the \(X_i\)’s are independent, the covariance is zero, and that means \(\langle Z_i, Z_j \rangle = 0\). Thus \(Z_i, Z_j\) are “orthogonal”, and so \(\frac{1}{n}\sum_{i=1}^n Z_i = \frac{1}{n}\sqrt{\sum_{i=1}^n \lVert Z_i \rVert^2} = \sqrt{\frac{\text{Var}(X)}{n}} = O\left(\sqrt{\frac{1}{n}}\right)\).</p> <p>Once you can bound the variance of a random variable, you can prove tail bounds using <a href="https://en.wikipedia.org/wiki/Chebyshev%27s_inequality">Chebyshev</a>. This gives us the kinds of convergence bounds I talked about in the beginning.</p> <p>So there we have it. Two seemingly different things are actually the same once we look at them using the right abstractions.</p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:pythagoras" role="doc-endnote"> <p>Does this mean Pythagoras theorem is trivial once you have discovered inner product spaces? It is quite true that if we think of line segments in 2d as 2-dimensional vectors, and define the inner product in the usual linear algebra way, and define norm-squared as the inner product with itself, then equation \(\eqref{eq:pythagoras}\) holds. However, the content of Pythagoras theorem is to show that what we think of as a “right angle” is the same thing as the inner product being zero, and what we think of as “length” is the same thing as the norm. <a href="#fnref:pythagoras" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><summary type="html"><![CDATA[At some point I used to do computational/combinatorial geometry and I’d have square roots appear in my calculations a lot. Most of the time they appeared due to the Pythagoras theorem. For example, the long diagonal of the \(n\)-dimensional cube circumscribing the unit ball, has length \(2\sqrt{n}\).]]></summary></entry><entry><title type="html">Faster than O(1/\sqrt{n}) rate for agnostic learning?</title><link href="https://vinayakpathak.github.io/blog/2026/faster-than-o1-sqrt-n-rate-for-agnostic-learning/" rel="alternate" type="text/html" title="Faster than O(1/\sqrt{n}) rate for agnostic learning?"/><published>2026-04-02T22:39:44+00:00</published><updated>2026-04-02T22:39:44+00:00</updated><id>https://vinayakpathak.github.io/blog/2026/faster-than-o1-sqrt-n-rate-for-agnostic-learning</id><content type="html" xml:base="https://vinayakpathak.github.io/blog/2026/faster-than-o1-sqrt-n-rate-for-agnostic-learning/"><![CDATA[<p>The story of binary classification (and many other learning problems) is that in the realizable case, we get a learning rate of \(O(1/n)\), however for agnostic learning it’s not possible to get anything better than \(\Omega(1/\sqrt{n})\). Right? Right?</p> <p>Ok, here’s a learning problem where we can get \(O(1/n)\) in the agnostic case. I don’t know if there is a characterization for exactly when this is possible, but here is the problem.</p> <p>Suppose your data distribution \(D\) over \(X \times Y\) is such that the marginal over \(Y\) is uniform over \(\{-1, 1\}\). Let the hypothesis class be the set of constant functions \(\mathcal{H} = \{h_c : X \to [-1, 1] \mid h_c(x) = c \text{ for all } x \in X,\ c \in [-1,1]\}.\) Let the loss be the quadratic loss, namely \(\ell(h, x, y) = (h(x) - y)^2\). If the training sample is \(S = ((x_1, y_1), \ldots, (x_n, y_n)) \sim D^n\) and we write \(\bar{Y} = \frac{1}{n}\sum_{i=1}^n y_i,\) then ERM returns the constant predictor \(h_{\bar{Y}}\).</p> <p>On the other hand, the optimal hypothesis for the population risk is clearly \(h_0\), and its risk is \(R(0)=1\). In particular, this is genuinely agnostic since the optimum risk is not zero.</p> <p>The risk of ERM is \(R(\bar{Y}) = \mathbb{E}_{(x,y)\sim D}\left[(\bar{Y}-y)^2\right] = \bar{Y}^2 - 2\bar{Y}\mathbb{E}[Y] + \mathbb{E}[Y^2] = \bar{Y}^2 + 1,\) where we used the facts that \(\mathbb{E}[Y]=0\) and \(\mathbb{E}[Y^2]=1\).</p> <p>And therefore, the excess risk is \(R(\bar{Y}) - R(0) = (\bar{Y}^2 + 1) - 1 = \bar{Y}^2.\) Taking expectation over the randomness of the training set, \(\mathbb{E}_S[R(\bar{Y}) - R(0)] = \mathbb{E}[\bar{Y}^2].\) But \(\bar{Y}\) is the average of i.i.d. random variables with mean \(0\) and variance \(1\), hence \(\mathbb{E}[\bar{Y}^2] = \mathrm{Var}(\bar{Y}) = \frac{1}{n}.\) So the expected excess risk of ERM is exactly \(\mathbb{E}_S[R(\hat{h}_{\mathrm{ERM}}) - \inf_{h\in\mathcal{H}} R(h)] = \frac{1}{n}.\)</p> <p>Thus this agnostic learning problem has rate \(O(1/n)\).</p>]]></content><author><name></name></author><summary type="html"><![CDATA[The story of binary classification (and many other learning problems) is that in the realizable case, we get a learning rate of \(O(1/n)\), however for agnostic learning it’s not possible to get anything better than \(\Omega(1/\sqrt{n})\). Right? Right?]]></summary></entry><entry><title type="html">What do Newton’s laws really say?</title><link href="https://vinayakpathak.github.io/blog/2024/newton/" rel="alternate" type="text/html" title="What do Newton’s laws really say?"/><published>2024-12-24T13:52:16+00:00</published><updated>2024-12-24T13:52:16+00:00</updated><id>https://vinayakpathak.github.io/blog/2024/newton</id><content type="html" xml:base="https://vinayakpathak.github.io/blog/2024/newton/"><![CDATA[<p>I recently read this Feynman quote:</p> <blockquote> <p>Study hard what interests you the most in the most undisciplined, irreverent and original manner possible.</p> </blockquote> <p>And decided to express some long-suppressed irreverance on this blog: Newton’s laws of motion are a piece of terrible writing. They do not really encapsulate the work that Newton had actually done. If I was a coauthor of the paper with him, I would write a comment in red in the overleaf. If this was a github pull request, I would refuse to merge it until the laws were rewritten. For reference, here are the laws from Wikipedia:</p> <ol> <li>A body remains at rest, or in motion at a constant speed in a straight line, except insofar as it is acted upon by a force.</li> <li>At any instant of time, the net force on a body is equal to the body’s acceleration multiplied by its mass or, equivalently, the rate at which the body’s momentum is changing with time.</li> <li>If two bodies exert forces on each other, these forces have the same magnitude but opposite directions.</li> </ol> <p>Let’s first start with the obvious issue here. The first law sounds like a weaker form of the second law? The second law already says \(F = ma\). If you assume \(F=0\), then as long as the body exists (and thus has non-zero mass), the only possibility is that \(a = 0\). And by the definition of acceleration, \(a = 0\) implies that the velocity does not change. So law #1 is actually implied by law #2. This is like if fight club actually had two rules. First rule: do not talk about fight club. Second rule: actually, all talking is prohibited. I can understand wanting to draw attention to a specific consequence of a law you formulated. For example, just saying \(F=ma\) may not always immediately convey to the reader all of its consequences, and you may want to draw their attention to how the absence of force means velocity does not change. But in today’s parlance, such a statement would be considered a corollary and I wouldn’t mind if law #1 was actually stated as a corollary.</p> <p>The second issue is more subtle but also bigger. To explain, I need to first take a step back. The thinking in Newton’s time was that motion was the most fundamental thing to explain and the whole universe was simply a large number of tiny things moving around. Sometimes the tiny things got together and formed big things and other times they remained tiny, but the belief was that any phenomenon could be explained by the exact motion of the big and small things constituting it. This program did appear to be successful for a long time. The movements of celestial objects were explained, a fairly reasonable theory of electricity was formulated that relied on charged particles moving under electrostatic forces, and even phenomena like heat and cold were explained using the motion of tiny molecules bumping into each other.</p> <p>So let’s try to put ourselves in Newton’s shoes and try to formulate a theory of motion, with the hope that once we have a theory of motion, everything will be explainable. Our task is this: can we formulate a theory that can predict the trajectories of objects that are moving around? You see some objects in space, and you are allowed to measure any properties of the objects and the space they are in, and with this information you should be able to predict where the objects will be at each point in time in the future. Newton’s claim was that all you needed to do was to measure the object’s mass and the force acting on it and that would tell you its acceleration and you could integrate that twice to get its position. And now I can state my second issue with the laws: up until now, force and mass are both made up quantities and thus the first two laws have basically no content! Or in other words, if the first two laws were all you were equipped with, and you saw a new kind of trajectory being followed by a collection of objects, you could simiply “explain” it by fitting whatever \(F\) and \(m\) resulted in that particular trajectory. So, for example, if you carefully observe an object for an hour and notice that its acceleration follows \(a = x^2 + y^2 - \sqrt{t}\), you say “ah, this is because the object has unit mass and it’s being acted on by a force \(F = x^2 + y^2 - \sqrt{t}\). In other words, there are no trajectories that are ruled out by these laws. It doesn’t exactly qualify to be a law if it allows every possible behaviour, does it?</p> <p>I am being a little facetious here. Force and mass weren’t <em>completely</em> made up quantitites. They were defined based on intuitions from the everyday world. We do observe that to move an object we have to go and push (or pull) it. Force is literally the word used to describe this act. We need to apply force to move things. And so Newton thought that if something was accelerating, something must have been applying a force on it. Mass is our intuitive understanding of how difficult it is to move something. Heavier objects are harder to move than lighter objects. And so it was hoped that mass represented a similar quantitity in Newton’s laws. While these intuitions do not give us a precise way of measuring mass or force directly, they do help us make the necessary assumptions that give Newton’s laws some actual predictive power. For example, based on our intuition about mass, we would probably postulate that the mass of an object remained constant with time. Similarly, based on our intuition about force, we would probably have opinions on the variables that a force is a function of. Suppose we want to predict the motion of an object tied to one end of a spring whose other end is tied to a wall. We might think that the force applied by the spring on the object is a function of how stretched the spring is and nothing else. Thus if we tie the object to the spring, stretch out the spring, and let it go, we would expect that the spring would start its oscillatory motion and that every time it reached a stretch of \(x\), the acceleration of the object would be \(f(x)\) for some fixed function \(f\). This is precisely because we think that: a) the mass of the object doesn’t change, and b) the spring force is a function only of the stretch \(x\). Thus with these two assumptions, Newton’s first two laws do rule out several trajectories. In particular, they say that we cannot see a trajectory where the spring attains the same stretch \(x\) twice but the object has a different acceleration each time. This can then be used to come up with predictions. We can measure several \((x, a)\) pairs, where \(x\) is the stretch and \(a\) is the acceleration of the object, and we can fit a curve through the points we observe. When we do that we will get something like \(a = -kx\) for some constant \(k\). And this equation lets us make predictions about what the acceleration will be of this specific object when tied to this specific spring and left at a stretch of \(x\).</p> <p>By the way, one of Newton’s many contributions was to use his scheme to predict motion of celestial objects. In particular, you can postulate that planets move because the sun is exerting a force on them which depends on: a) distance \(r\) of the planet from the sun, and b) some other properties of the planet that stay constant with time. Now you can point your telescope at a specific planet, say Saturn, and record many \((r, a)\) pairs where \(a\) is Saturn’s acceleration when it’s at distance \(r\). When you fit a curve through the data, you will find that \(a = C\cdot\frac{1}{r^2}\), for some constant \(C\) that depends on those other properties of Saturn that the force is supposed to depend on. This observation leads to the famous inverse-squared law of gravity. However, the weird thing is that if you pick another planet, say Jupiter and run the same experiment, you end up with the exact same expression \(a = C\cdot\frac{1}{r^2}\) (with the same value for \(C\)). You can run this with any number of planets, or even asteroids going around the sun and end up with the exact same expression for all of them. But \(C\) was supposed to depend on properties of the planet! Does this mean all planets are identical when it comes to gravity? In fact, the coincidence that makes this happen is that the property of the planet that this force is supposed to depend on is exactly the planet’s mass. In fact, you can write \(F = C'\cdot\frac{m}{r^2}\) where \(m\) is the mass of the planet. Since \(a = F/m\), the \(m\) cancels out and you end up with an expression that does not depend on \(m\). The thing that a force depends on, and the thing that determines acceleration given a force are the same thing? This is too much of a coincidence! Honestly I feel that this should have set off alarm bells in the apple orchards of Cambridge and the “force” theory of motion should have sounded suspicious. But I guess Newton had done more than enough for one life.</p> <p>So far we have seen that even though force and mass are made up quantities, if we assume some additional structure on them that were not specified by Newton’s laws, the laws do start to characterize the kinds of motion that are legal in the universe. However, the third law changes that by removing the need to have a lot of extra structure. We still need to assume that mass doesn’t change (and perhaps we can chalk that up to common sense) but we don’t need to assume anything extra about the force. The third law provides that extra constraint that needs to be satisfied by all forces. It basically states that force isn’t a property of one object but of two objects. It’s not that one object exerts a force on another object but that two objects exert a force on each other. It’s not that the sun pulls Saturn towards itself, but that the sun and Saturn pull each other towards themselves. Thus given any two objects \(A\) and \(B\), you get one number (not two) \(F\) describing the force between them. And the nature of \(F\) is that the direction in which it acts on \(A\) is the opposite of the direction in which it acts on \(B\). So now imagine two objects that are in motion due to the force between them. Suppose that there are no other forces acting on them currently. Then if we combine the second and third laws, we get that \(m_1a_1 = -m_2a_2\) at all time, where \(m_1, m_2\) are the masses and \(a_1, a_2\) are the accelerations of the two objects. This can be rearranged to say \(m_1a_1 + m_2a_2 = 0\). Realizing that acceleration is the derivative of velocity wrt time, we get \(\frac{d}{dt}(m_1v_1 + m_2v_2)=0\), which gives us the familiar law of conservation of momentum. That is, the total momentum \(m_1v_1 + m_2v_2\) of the system does not change with time. This is a pretty strong requirement on what trajectories are considered legal by nature’s laws.</p> <p>The characterization in terms of momentum still refers to mass, which is a made up quantity. So what exact trajectories does it rule out? Can we still explain any trajectories by simply fitting appropriate values of mass in the equations? Imagine two objects \(A\) and \(B\) moving under the influence of each other. Suppose there are no other external forces acting on them. You can then observe their velocities \(v_1(t)\) and \(v_2(t)\) for several values of \(t\). Suppose you measure it for \(k\) different values. Conservation of momentum gives you \(k-1\) equations, but we have only two unknowns: \(m_1\) and \(m_2\). Newton’s laws say that these large number of equations in two variables are consistent with each other.</p> <p>A law that I think is missing from Newton’s original list is that forces are additive. So if \(A\) and \(B\) exert forces \(F\) and \(-F\) on each other, and \(A\) and \(C\) exert forces \(F'\) and \(-F'\) on each other, then the total force on \(A\) will be \(F+F'\). This isn’t obvious and one could imagine complex non-linear interactions taking place, but it is indeed the case that complex non-linear interactions do not occur and forces add up nicely.</p> <p>So I would probably write the laws of motion like this:</p> <ol> <li>The universe is made up of a large number of objects moving around under each other’s influence.</li> <li>Each object \(i\) has associated with it a quantity \(m_i\) called mass that does not change with time.</li> <li>Each pair \((i, j)\) of objects has associated with it a quantity \(F_{ij}\) called force that can change with time but must always satisfy the property that \(F_{ij} = -F_{ji}\).</li> <li>At any point in time, the acceleration of object \(i\) can be calculated as \(a_i = F_i/m_i\) where \(F_i = \sum_j F_{ij}\).</li> </ol>]]></content><author><name></name></author><category term="physics"/><summary type="html"><![CDATA[I recently read this Feynman quote: Study hard what interests you the most in the most undisciplined, irreverent and original manner possible.]]></summary></entry></feed>